/** postMessage / BroadcastChannel payload type after vendors-embed save. */
export const INVOICE_VENDORS_EMBED_SAVED = "food-costing:invoice-vendors-saved";

/** Same-origin tab sync when window.opener is unavailable (e.g. noopener). */
export const INVOICE_VENDORS_BROADCAST_CHANNEL =
  "food-costing:invoice-vendors-sync";
